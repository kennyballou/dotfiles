;;; init-c --- C/C++ configuration
;;; Commentary:
;;; Code:

(defvar c-default-style)
(setq c-default-style `((java-mode . "java")
                        (awk-mode . "awk")
                        (c-mode . "linux")
                        (other . "gnu")))

(provide 'init-c)
;;; init-c.el ends here
